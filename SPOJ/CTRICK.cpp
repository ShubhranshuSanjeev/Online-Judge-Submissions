#include <cstdio>
#include <cstring>
#include <iostream>
#include <vector>
#define MAX 20007
using namespace std;

int fen_tree[MAX];

int main(){
    int t, n;
    scanf("%d", &t);
    while(t--){
        scanf("%d", &n);
        memset(fen_tree, 0, sizeof(fen_tree));

        
    }

    return 0;
}